# 依赖
node_modules

# 构建产物
dist
dist-ssr
dev-dist

# 本地文件
*.local
*.log
.DS_Store

# 交付前自检临时脚本（每次交付前删除，见开发手册 §11.4）
.tmp-*

# CloudBase CLI 的部署状态（机器本地，2026-09-12 起；不含凭据，登录态在用户目录）
.tcb/
.cloudbase/

# 编辑器
.idea
