# Xcode 的用户态数据：断点、窗口位置、打开的文件、上次跑的 scheme…
# 纯本地状态，没有一行是工程本身，提交上去只会每次 diff 都在变。
xcuserdata/
*.xcuserstate
*.xcscmblueprint

# 构建产物（本工程用 xcodebuild -derivedDataPath 指到别处，这里是兜底）
build/
DerivedData/
*.dSYM

# macOS
.DS_Store
