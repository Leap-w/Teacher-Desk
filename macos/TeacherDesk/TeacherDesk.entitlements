<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
    宿主 App 的 entitlements —— 刻意是空的。

    这个 App 不上架、只自己用，所以**不开 App Sandbox**：
    沙箱要配的东西更多，而且会挡住「宿主 App 与 Widget 共享一个普通目录」这条最简单的路。
    不开沙箱之后，`com.apple.security.network.client` 这类键也没有意义了（它们是沙箱能力），
    一并去掉——宿主 App 照样能联网，不受影响。

    快照文件因此放在 ~/Library/Application Support/TeacherDesk/，见 Shared/SnapshotStore.swift。

    哪天要用回沙箱 + App Groups（比如真上架），在这里加回来：
        com.apple.security.app-sandbox            = true
        com.apple.security.network.client         = true
        com.apple.security.application-groups     = [ group.com.teacherdesk.mac ]
    并同步改 TeacherDeskWidget.entitlements 与 SnapshotStore 的路径实现。
-->
<plist version="1.0">
<dict/>
</plist>
