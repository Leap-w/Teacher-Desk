# TeacherDesk 开发计划（进展分析 + 后续安排）

> 本文档由 2026-09-10（tag `v0.8.0` 交付后）的整体进展分析整理而成，用于独立承载**进度评估与后续计划**；同日 `v0.9.0`（数据管理增量）、`v0.10.0`（Phase 6 值日管理）交付时同步。
> 与 `docs/roadmap.md` / `docs/开发手册.md` §10 的分工：roadmap / §10 是**阶段表**（每个 Phase 做什么），本文是**评估与排期**（做到哪了、风险在哪、下一步做什么、什么需要拍板）。三者冲突时以开发手册为准。
> 交付物的逐项明细见 `docs/CHANGELOG.md`；文中的规模 / 构建数字随每次交付更新（口径写在表内）。

---

## 一、总体结论

项目已从「学生档案 + 排座」的工具集，长成**班主任每天可以真正打开用的工作台**：首页（今日课程 / 待办 / 请假审批 / 今日值日）是入口，背后挂着六个成型的业务模块。

三点判断：

1. **日常可用度约 90%**——高频动作（看课、记事、查人、排座、批假、排值日）已闭环；缺的主要是**周期性事务**里的周末返家，以及考勤。
2. **最大风险（数据丢失）已从「无法应对」降为「需教师配合」**：全部数据仍在浏览器 localStorage，但自 `v0.9.0` 起可一键导出 / 合并导入备份、可清空示例数据开始录真实数据。**残余风险是「教师想不起来导出」**——这是使用习惯问题，不是功能缺口（后续可在工作台加备份提醒，见 §五 #7）。
3. **工程状态健康**：七个 store 同一套返回契约、类型 / 规则 / 视图三层分明、无第三方 UI 库、四项验证 + 交付前独立审查已固化为每个阶段的必走流程（§11.4）；占位页自 Phase 6 起**归零**，每个一级导航都是可用的真实页面。

---

## 二、进度全景

| 阶段          | 交付                                                  | tag                   | 状态 |
| ------------- | ----------------------------------------------------- | --------------------- | ---- |
| Phase 0–1     | 工程框架 + UI Kit 设计系统                            | —                     | ✅   |
| Phase 2 / 2.1 | 学生档案 MVP + 模型收敛（清理 boarding 遗留）         | v0.2.0 / v0.2.1       | ✅   |
| Phase 3A–3D   | 座位管理四步（模型 → 拖拽 → 教室工具 → 自动排座）     | v0.3.0-alpha … v0.5.0 | ✅   |
| Phase 4       | 工作台（今日课程 / 待办 / 快捷入口 / 本周课时）       | v0.6.0                | ✅   |
| Phase 4.1     | 课程中心（周课表 + 编辑 + 临时代课 + 工作台联动）     | v0.7.0                | ✅   |
| Phase 5       | 请假 / 离校管理（申请 + 审批 + 离校返校登记）         | v0.8.0                | ✅   |
| 近期增量      | 数据管理（导出备份 / 合并导入 / 清空示例 / 清空全部） | v0.9.0                | ✅   |
| Phase 6       | 值日管理（值日组 + 按天轮换 + 今日值日卡片）          | v0.10.0               | ✅   |
| Phase 7       | 周末管理（返家申请 / 审批 / 离校返校 / 统计）         | —                     | ⏭    |
| Phase 8       | 首页仪表盘整合（剩余：班级概况卡片）                  | —                     | ⏭    |
| Phase 9       | 后端接入（腾讯云 CloudBase + PostgreSQL / 鉴权）      | —                     | ⏭    |
| Phase 10      | macOS Widget（SwiftUI + WidgetKit）                   | —                     | ⏭    |

> **编号说明**：「Phase 5」在本项目里有两种口径——① 路线图编号：请假 / 离校管理（`v0.8.0`）；② 交付口径：课程中心（路线图编号 Phase 4.1，`v0.7.0`）交付时需求方曾称其为「Phase 5」。**以 tag 为准**（详见开发手册 §10 注 1）。

**节奏**：自 2026-09-08 起 3 天内交付 11 个 tag（`v0.2.0` … `v0.10.0`，含 1 个 alpha）；平均每个阶段一个工作时段完成（含四项验证与交付前独立审查）。

---

## 三、已完成内容（按「能给班主任做什么」）

### 1. 学生档案 ✅

62 人班级的底账。搜索（姓名 / 学号 / 宿舍 / 标签）+ 筛选、重名区分、详情 / 新增 / 编辑弹窗、软删除、学号唯一性双层校验；**家庭地址 + 家庭所在地（返家范围）已入库**——这是 Phase 7 周末管理的数据地基。

### 2. 座位管理 ✅（四个子阶段，功能最重的一块）

- **3A** 真实教室模型（7 排 × 9 列 · 3-3-3 分列 · 63 座）+ 老师 / 学生双视角网格 + 多方案管理
- **3B** 原生拖拽换座（交换 / 移入空位）+ 换座日志与「本次调整」摘要 + 长按信息卡
- **3C** 座位图导出 PNG / PDF、学生定位、约束检查器、方案对比（含对比 PDF）
- **3D** **自动排座**：硬约束必满足（不满足逐条报冲突）、软规则尽量满足、同种子结果确定、生成新方案可对比可撤销

### 3. 工作台 ✅

首页成为默认入口：今日课程（按星期自动切）、今日待办（点击完成、刷新保持）、快捷入口、本周课时统计、日期头部（30 秒刷新，跨零点翻篇）。

### 4. 课程中心 ✅

`/schedule` 从只读数据源升级为**可编辑周课表**：手机分日列表 / PC 完整周视图、空格点选即新增、时段冲突拦截、临时代课标记；工作台与课表读同一份数据，增删改即时同步。

### 5. 请假 / 离校 ✅

`/leave` 覆盖**申请 → 审批 → 离校 / 返校登记**一条线：按「天 + 上午 / 下午」记录并自动算时长（跨月 / 跨年正确）；三态流转（待处理 → 批准 / 驳回）；「未离校 / 已离校 / 已返校」由时间戳派生；仅已批准可登记且校验时间线；工作台「请假审批」卡片同步待处理与本月已批准；学生被删除时记录保留、姓名快照冻结。

### 6. 工具箱 · 数据管理 ✅

`/toolbox` 从占位页升级为**数据备份与恢复**入口（不新增一级导航）：数据块的条目概览（交付时为六个，Phase 6 起七个——新增「值日安排」）；**导出备份（JSON）**（文件名带日期，记「上次导出」时间）；**合并导入**（按 id 合并：本机独有保留、备份独有追加、同 id 以备份为准，导入前先给出「新增 / 覆盖 / 保持不变」预览）；**清空示例数据**（列出待删名单后可一键清掉示例学生 / 课程 / 待办 / 请假及其座位约束，保留教师自己录入的内容）；**清空全部数据**（恢复到首次打开的状态）。数据全部丢失的红灯风险由此转为可控。

---

## 四、工程状态

> 下表截至 `v0.10.0`（2026-09-10）；规模与 PWA 数字来自本次交付的 `find` 统计与 `npm run build` 输出，随每次交付更新。

| 维度     | 数据                                                                                                                              |
| -------- | --------------------------------------------------------------------------------------------------------------------------------- |
| 规模     | 96 个源文件 / **17,220 行**（`src/**/*.{ts,vue}`；views 60% · utils 13% · components 11% · stores 10% · types 2%）                |
| 技术栈   | Vue 3.5 + TS 5.8 strict + Vite 7 + Router 4 + Pinia 3 + UnoCSS + vite-plugin-pwa                                                  |
| 数据流   | Pinia → localStorage，七个可写 store（student / seat / constraint / timetable / dashboard / leave / duty），键前缀 `teacherdesk:` |
| 外部依赖 | 6 个运行时依赖（含 html-to-image / jsPDF 用于导出），**无 UI 组件库**（`src/components/ui/` 自研 12 个组件）                      |
| PWA      | 43 个 precache 条目 / 1176.96 KiB，可安装（图标为 SVG 占位）                                                                      |
| 质量保障 | 每阶段：四项验证（prettier / vue-tsc / eslint / build）+ 交付前独立审查 + 数据层运行时自检 + SSR 渲染验证 + 文档同步              |

**结构解读**：视图层占 60%，是「自研组件 + 手机 / PC 双布局」的必然结果；承载业务规则的 stores + utils + types 约 4,370 行、占 25%，**规则集中在数据层**——这也是新模块能直接复用既有 store 契约（`undefined` = 被拒、`false` = 不存在）的原因。

---

## 五、已知问题与技术债（按会不会影响下一阶段排序）

| #   | 事项                                                                                                                              | 影响                                              | 计划                    |
| --- | --------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------- | ----------------------- |
| 1   | ~~**数据无导出 / 备份**，localStorage 一清即空~~ → `v0.9.0` 已提供导出 / 合并导入                                                 | ✅ 已解决（残余：靠教师记得导出）                 | 已完成                  |
| 2   | 七个 store 均「整份数组写回」，多标签页 / 已装 PWA 与浏览器同时开会丢更新                                                         | 🟡 单标签使用无感，多入口会踩                     | Phase 9 或提前          |
| 3   | ~~示例数据无「一键清空」入口~~ → `v0.9.0` 已提供「清空示例数据 / 清空全部数据」                                                   | ✅ 已解决                                         | 已完成                  |
| 4   | 已验收组件字号硬编码（10–22px 字面量）未 token 化                                                                                 | 🟢 一致性问题                                     | 择机（专门整理 UI Kit） |
| 5   | 同一模块三种称呼（侧栏「课程表」/ 页头「我的课表」/ 手册「课程中心」）                                                            | 🟢 文案一致性                                     | 择机                    |
| 6   | 删除确认文案在弹窗关闭动画期短暂变空；`viewport-fit=cover` 缺失；课程抽屉未接线回车保存；`services/index.ts` 注释超前             | 🟢 均记录在案、有意未修（理由见手册 §9.8）        | 择机                    |
| 7   | 备份是**手动**的：没有任何提醒机制，教师可能长期不导出                                                                            | 🟡 决定备份的实际保护效果                         | 择机（工作台备份提醒）  |
| 8   | 「清空示例数据」按 **id 前缀**识别示例记录：被教师就地改成真实内容的示例记录也会被删（确认弹窗已列出名单并明示）                  | 🟡 已用文案与名单兜住；根治需给种子记录加标记字段 | 择机                    |
| 9   | 概览数字读的是**磁盘上已存在的键**：若某模块从未打开过（直接深链进 `/toolbox`），该块显示 0，与打开该模块后的实际（示例）条数不符 | 🟢 罕见路径，不影响数据                           | 择机                    |
| 10  | 旧课表键 `teacherdesk:timetable:lessons` 存续时（迁移有丢弃条目才会保留）不被「清空示例数据」清理，可能残留示例课程原文           | 🟢 数据不会丢；「清空全部数据」会一并清掉         | 择机                    |

---

## 六、后续开发计划

### 近期增量：数据管理 ✅（`v0.9.0` 已交付）

**动因**：localStorage 是当前唯一数据载体，清缓存 / 换设备即全部丢失；这是本项目唯一的红灯级风险。

**拍板与落地**（需求方决定）：

- **导入策略 = 合并式**（不是覆盖式）：本机独有的记录原样保留、备份独有的追加、同 id 以备份为准；**合并从不删除当前数据**。为了不让「同 id 覆盖」悄悄发生，导入前先给出**逐块预览**（新增 / 覆盖 / 保持不变）。
- **清空 = 两档**：清空示例数据（保留教师自己录入的内容）+ 清空全部数据（回到首次打开的状态）。
- **交付 tag = `v0.9.0`**；入口 `/toolbox`（不新增一级导航）。

**实现取舍（详见 CHANGELOG 与开发手册 §9.12）**：备份按**存储层**读写原始 JSON，条目级校验仍由六个 store 的 `normalize*` 在重载后把关（不写第二套规则）；写盘整批原子化，失败即回滚，界面不得对教师说「数据未改变」而实际写了一半；缺 id 的条目照常导入（追加），读不动的数据块绝不写盘。

**已知边界**：手动备份（无提醒，见 §五 #7）；示例识别按 id 前缀（§五 #8）；概览数字只看磁盘已存在的键（§五 #9）；旧课表键不在「清空示例数据」范围内（§五 #10）。

### Phase 6 值日管理 ✅（`v0.10.0` 已交付）

**动因**：把每周要重复交代的事固化下来——值日表排一次、之后每天自动轮到谁清清楚楚。

**拍板与落地**（需求方 2026-09-10 定，六个问题逐条回答）：

- **轮换粒度 = 按天**；**只排「值日组」不分工位**（扫地的组与拖地的组不分家）；**本阶段不做打卡**，先只做编排与轮换；**分组由教师手动指定**（不按座位区 / 小组自动切分）；**周末是否值日做成开关由教师自定**（默认关＝周六周日不排，周末不消耗轮换位）；**工作台加一张「今日值日」卡片**。
- **交付 tag = `v0.10.0`**；入口 `/duty`（不新增一级导航）；**最后一个占位页消失**，`PagePlaceholder` 组件与文档引用一并删除。

**实现取舍（详见 CHANGELOG 与开发手册 §9.13）**：值日组与轮换设置**同存一个数组**（`kind` 区分），备份 / 合并 / 清空 / 概览因此零改动复用；轮换用「起点日期 + 起点组」一个基准推进（不存整张历史表），起点之前的日子按同一基准往前倒推；**日期算术逐日推进而不是「天数差减周末数」**——算错一次就是整张表错位，代价是起点年份必须设闸（2000–2099，读 / 写 / 纯函数三处都挡）。

**已知边界**：无打卡与完成情况统计、无工位分工、无按周 / 按月轮换、无节假日与调休日历（调休后需教师手动把起点改回今天）、无同一天多场景并行、无打印导出与提醒；起点组被删时轮换自动顺延但要教师确认。

### Phase 7 周末管理（下一路线图阶段）

| 项       | 内容                                                                                                                        |
| -------- | --------------------------------------------------------------------------------------------------------------------------- |
| 目标     | 返家申请、审批、离校 / 返校登记、周末统计                                                                                   |
| 规模     | 中（估）                                                                                                                    |
| 依赖     | 学生档案的**返家范围已入库**（`familyLocation.scope`）；**流程骨架可复用 Phase 5**                                          |
| 架构建议 | 周末返家 = 申请 → 审批 → 离校 / 返校登记，与请假**同构**；建议先把 Phase 5 的流程骨架抽出复用，避免出现第二套几乎一样的实现 |
| 概念边界 | 档案存「家庭所在地事实」，周末管理存「某个周末的行为结果」（开发手册 §2.2）                                                 |

### Phase 8 首页仪表盘整合

| 项   | 内容                                                                                                                                |
| ---- | ----------------------------------------------------------------------------------------------------------------------------------- |
| 剩余 | 只剩「班级概况」卡片接入真实数据（人数 / 出勤 / 值日汇总）                                                                          |
| 说明 | 今日课程 / 待办 / 快捷入口 / 周课时（Phase 4）与请假审批（Phase 5）已完成；「出勤」依赖考勤模块（未排期），届时可先接人数与值日汇总 |
| 规模 | 小（估）                                                                                                                            |

### Phase 9 后端接入

| 项   | 内容                                                                                      |
| ---- | ----------------------------------------------------------------------------------------- |
| 目标 | 腾讯云 CloudBase + PostgreSQL；`services/` 抽象层替换 localStorage 实现；鉴权与多班级支持 |
| 规模 | **大**（估）：六个 store 全量改造，是迄今最大的一次结构变动                               |
| 建议 | 一并纳入「跨标签页同步」与「备份导出」（后端就绪后备份仍应保留，作为离线兜底）            |
| 时机 | 建议在模块相对完整（Phase 6–8 之后）再做，避免改造期间反复调整                            |

### Phase 10 macOS Widget

| 项   | 内容                                                  |
| ---- | ----------------------------------------------------- |
| 目标 | SwiftUI + WidgetKit 桌面小组件                        |
| 依赖 | 强依赖 Phase 9；数据源方案（本地文件 / 短 URL）需探讨 |
| 规模 | 中（估）                                              |

### 未排期候选（按需插入）

- 工具箱小工具：随机点名、随机分组、课堂倒计时
- PWA 完善：正式 PNG 应用图标（当前 SVG 占位）、离线体验打磨、更新提示
- 考勤记录（请假模块已明确不含考勤；「班级概况」的「出勤」依赖它）

---

## 七、开发与维护约定

- 每个阶段的开工 → 实现 → 验证 → 审查 → 交付流程见开发手册 **§11.4**（四项验证 + 交付前独立审查 + 文档同步 + 一次提交一个 tag）
- 本文档与 `roadmap.md` / 开发手册 §10 需在**每个阶段交付后同步**；阶段表的权威版本是开发手册 §10
- 待拍板事项由需求方决定后写入对应阶段的小节，再开工
