# TeacherDesk 发布检查清单（Release Checklist）

> 用途：**正式发布前逐项过一遍**。每一条都是「能当场看见结果」的动作，不是原则口号。
> 当前状态：`v3.3.1-rc`（Polish 收尾候选）—— 本文末尾有 RC-1（`v2.3.1-rc`）的实测记录，作为「怎么验」的样例。
> 相关文档：[ARCHITECTURE.md](./ARCHITECTURE.md)（分层与数据流）、[cloudbase-setup.md](./cloudbase-setup.md)（云端配置）、[roadmap.md](./roadmap.md)（版本路线）。

---

## 0. 发布前必跑（等价于 CI）

`.github/workflows/ci.yml` 跑的就是下面这六步，本地按同样顺序跑一遍，全绿才算过：

```bash
npm ci
npx prettier --check .
npx eslint .
npx vue-tsc --noEmit
npm run test        # 常驻自检，必须全过
npm run build
```

- [ ] `prettier --check` 无输出（有输出就 `npx prettier --write .` 后重跑）
- [ ] `eslint` 零 error
- [ ] `vue-tsc` 零 error（**注意**：`declare const` 在模板表达式里不可见，全局常量走 `import.meta.env`）
- [ ] `npm run test` 全绿（当前 **665** 条 / 30 个文件）
- [ ] **SPA 深链占位**：`npm run build` 已自动生成 `<路由>/index.html`
      （`scripts/hosting-routes.cjs`；`src/__tests__/hostingRoutes.test.ts` 守护路由表一致性）；
      部署后抽查两个子路由：直接刷新不 404、返回的 HTML 指向**本次**构建的 chunk
      （占位指向旧 chunk = 上一版事故，见 CHANGELOG 2026-09-14 部署修复）
- [ ] **深色模式**：三档（浅色/深色/跟随系统）各抽查首页与「我的」；深色下零硬编码白底/黑字
      （全走 `[data-theme='dark']` 令牌覆盖）；座位导出 PNG 仍为浅色纸张（.theme-force-light）
- [ ] 测试时区已固定（`vitest.config.ts` 的 `env.TZ`）——不得依赖测机时区，否则本机绿、CI 红
- [ ] `npm run build` 成功，且 `dist/` 里没有上次的旧产物（先 `rm -rf dist`）

## 1. 版本与文档一致性

- [ ] `package.json` 的 `version` 已改（**唯一来源**：页脚 / 关于页 / 备份元信息都构建期读它）
- [ ] `docs/CHANGELOG.md` 有对应版本条目，日期与 tag 对得上
- [ ] `docs/roadmap.md` 的阶段状态已更新（交付打 ✅）
- [ ] `docs/开发手册.md` 有对应 §9.x 记录
- [ ] 打完 tag 后页脚显示的就是新版本号（构建期注入，不是硬编码）

## 2. 数据安全（最不能出错的一节）

- [ ] **备份可导出**：我的 → 工具箱 → 导出数据，文件能下载、能打开
- [ ] **导出可导入回来**：改动数据 → 导入刚才那份备份 → 数据回到备份时的样子
- [ ] `teacherdesk:*` 键名**没有变化**（老数据自动可用，不需要教师重新导入）
- [ ] 清空本机数据后重新打开：示例数据照常播种，不报错
- [ ] 播种基线（`teacherdesk:seedText`）与写盘时刻表（`teacherdesk:localWriteAt`）随「清空」一起清掉

## 3. 云同步（Local First + Single User）

- [ ] 未配置环境 ID 时**完全本地**：不报错、不发请求、首页无同步提示条
- [ ] 邮箱登录 → 自动进入同步；刷新页面自动登录仍然有效
- [ ] 登出后队列与对齐记账被清掉（换账号不会串数据）
- [ ] **首次同步四态**：两边都空 → 正常开始；本机有云端空 → 弹窗问一句再初始化；云端有本机空 → 直接拉；两边都有 → 逐键裁决
- [ ] 本机改一次 → 云上有；另一台设备拉一次 → 拿到同一份
- [ ] **冲突无死循环**：首次同步两边都改过 → 一个字不动 + 列进待裁决，重复同步不产生新推送
- [ ] 断网改动 → 联网后自动补推、队列清空、状态回「已同步」（**不需要点任何按钮**）
- [ ] 刷新 / 关标签 / 重启后，待同步队列能续传且**不重复推**（`teacherdesk:sync-queue`）
- [ ] 工具箱 → 同步诊断：状态 / 队列长度 / 待同步 Key / 最近同步时间 / 最近错误 / 当前通道 六项都读得到

## 4. 长事务（导入与批量操作，RC-01 / RC-02）

- [ ] 座位 Excel 导入：预览数字与落库结果一致；错误行不写入；导入期间**同步暂停**，完成后**自动同步**一次
- [ ] 学生批量导入 / 批量修改：同一套锁与自动同步行为
- [ ] 课表导入 / 工作清单导入：同上
- [ ] 导入中导出、导入中再导入**不会互相打断**（操作锁是计数式的，可嵌套）

## 5. 关键流程现点一遍（班主任）

- [ ] 学生档案：搜索、重名消歧（填了尾号的重名显示「旦增卓玛（3287）」，不重名只有姓名）、编辑保存、软删除
- [ ] 座位管理：拖拽换座、Excel 导入、PNG 导出、PDF 导出、方案切换
- [ ] 请假：登记、返回、状态变化（记录制，无审批）
- [ ] 值日：今日值日、分组、轮换设置（设置项在页面最底部）
- [ ] 周末返家：地区筛选、登记、时间轴

## 6. 关键流程现点一遍（数学教师）

- [ ] 首页：下一节课 / 今日待办 / 班级动态，数据真实；空态有可读文案
- [ ] 课程表：Week View、当前课程高亮、Hero 显示当前/下一节
- [ ] 工作清单：新建、完成、今日统计与列表一致

## 7. 课堂工具（One-Tap Classroom）

- [ ] 进页面即可用：三个工具同屏，无折叠、无配置向导
- [ ] 随机点名：全班 / 男生 / 女生 / 今日值日组四种模式；**连点只抽一次**（防连点 1 秒）
- [ ] 计时器：四个预设 + 自定义；开始 / 暂停 / 重置；**切后台回来时间准确**；结束是页面轻提示
- [ ] 抽签：列出全部值日组，随机抽一组；滚动期间组按钮禁用
- [ ] 左上角「返回工作台」一键回工作台

## 8. 响应式（必须三尺寸都过）

在 1440 / 768 / 390 各过一遍**全部**页面（首页、学生、座位、请假、值日、周末、课程表、工作清单、我的、工具箱、课堂工具）：

- [ ] `document.documentElement.scrollWidth <= clientWidth` —— **零横向滚动**
- [ ] Hero 不溢出，FAB 不遮挡内容
- [ ] 课堂工具三卡：桌面三列 / 平板两列 / 手机单列

## 9. 可访问性基础

- [ ] Tab 能走完主要操作（按钮 / 输入 / 抽屉 / 弹窗）
- [ ] 焦点环可见（`:focus-visible` + `--ring-focus`）
- [ ] 纯装饰图标有 `aria-hidden`，有意义的图标有可读名称
- [ ] 辅助文字对比度 ≥ 4.5:1（`--color-text-tertiary` 已是 `#67767a`）

## 10. 上线与回滚

- [ ] `npm run build` 产物上传 CloudBase 静态网站托管（配置口径见 [cloudbase-setup.md](./cloudbase-setup.md)）
- [ ] 线上首屏打开、刷新（SPA 路由回退到 `index.html`）、PWA 安装提示正常
- [ ] **回滚预案**：本地 `git tag` 有上一个可用版本；线上回退用上一份构建产物
- [ ] **数据回滚预案**：导出备份文件在手；云端误覆盖时可从备份 + 云端文档时间戳逐键挽回

---

## 11. 死代码与分层体检（V3.0.0 起，每次大版本过一遍）

四条脚本化检查（**已入库在 `scripts/audit/`**，路径无关，换机器不用改），都能**跑出一份清单**，不靠眼睛看：

```bash
# ① 未引用的源文件 / 死导出 / 未使用依赖 / TODO / console / 临时文件
#    （输出为空即干净；「导出但只在本文件内用」属于风格问题，不是死代码）
node scripts/audit/dead-code.cjs && node scripts/audit/dead-exports.cjs

# ② 架构纪律：谁在直连底层（views/stores 直连 storage/sync/localStorage 应为空）
#    另含：定时器与事件监听是否成对清理 / JSON.parse 是否包 try / v-for 是否都有 key
node scripts/audit/layering-and-hygiene.cjs

# ③ 运行时：逐路由控制台报错 + 未捕获异常 + 404 + 可访问性抽查
#    （无头 Chrome + CDP，起服务与浏览器的写法见下方「附」）
node scripts/audit/runtime-check.cjs
```

**判定标准**：① 无死码（**有决策记录保留**的除外，且必须在文档里写明理由）；
② 页面层越层引用为零；③ 运行时零报错、可访问性抽查三项全零。

- [ ] 没有「导出后无人引用」的**函数 / 常量**（类型导出不在此列）
- [ ] `views/` 里没有 `@/services/*`、`@/sync/autoSync` 的直接 import
- [ ] 没有随旧 UI 移除后残留的令牌 / 别名（V3.0 清掉了 `--app-*` 与底部导航两项）
- [ ] 没有被下线功能的孤儿资源（图标 / 图片）——PWA 预缓存列表与实际文件一致

## 附 A：v3.0.0（正式版）实测记录

验证方式：**静态审计脚本 + 无头 Chrome/CDP 真机体检**（真实数据、真实交互）。

### 死代码与分层（三条脚本，清单为空即过）

| 检查                                                                                                     | 结果                                                            |
| -------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------- |
| 未引用的源文件                                                                                           | 0（`services/index.ts` 为有决策记录的保留项，见开发手册 §9.57） |
| 「导出后无人引用」的函数 / 常量                                                                          | 0（另有 2 处为有决策记录的保留项）                              |
| 未使用的依赖                                                                                             | 0                                                               |
| `TODO` / `console.log` / `@ts-ignore` / `as any` 残留                                                    | 0 / 0 / 0 / 0                                                   |
| 临时文件、`.tmp-*`、`.log`、`dist` 入库                                                                  | 0                                                               |
| 孤儿资源                                                                                                 | 0（删掉 7 个未被引用的 PWA 图标后，SW 预缓存从 14 项降到 7 项） |
| `views` / `components` / `stores` 直连 `services/*`、`@/sync/autoSync`、`@/repositories`、`localStorage` | **0**（V3.0 收口后首次成立）                                    |
| `v-for` 缺 `:key` / `<img>` 缺 `alt` / `JSON.parse` 未包 `try`                                           | 0 / 0 / 0                                                       |

### 运行时（11 条路由）

| 检查                                                                           | 结果      |
| ------------------------------------------------------------------------------ | --------- |
| console 报错 / 警告                                                            | 0         |
| 未捕获异常                                                                     | 0         |
| 请求失败（4xx / 5xx / 网络）                                                   | 0         |
| 可访问性抽查（首页 / 座位 / 工作清单 / 课堂）：无名按钮 / 无标签输入 / 重复 id | 0 / 0 / 0 |
| PWA：manifest 图标存在性、SW 预缓存列表与实际文件一致                          | ✓         |
| 功能冒烟：工具箱「导出备份」真实落盘（17KB、含全部已写入模块）                 | ✓         |

### 工程

| 项目                                          | 结果                                                  |
| --------------------------------------------- | ----------------------------------------------------- |
| 自检                                          | **553 条**全绿（新增 `sheetCell` 9 条，含跨时区断言） |
| prettier `--check` / vue-tsc / eslint / build | 全绿                                                  |
| CI（六步）                                    | main 与 tag 全过                                      |

**未覆盖（如实说明）**：真实 CloudBase 环境下的端到端同步仍未在本地验证（需线上账号与网络），
属于上线当天的人工步骤（见第 3、10 节）；常驻自检用「假云端 + 双设备模拟」覆盖同一套逻辑。

## 附 B：RC-1（`v2.3.1-rc`）实测记录

验证方式：**无头 Chrome + CDP 真机点一遍**（真实数据、真实交互），加上常驻自检。

| 项目            | 结果                                                                                                                               |
| --------------- | ---------------------------------------------------------------------------------------------------------------------------------- |
| 全路由 × 三尺寸 | **11 路由 × 3 尺寸 = 33 项**，零横向滚动、页面均正常渲染                                                                           |
| 关键元素        | 11 项全过（Dashboard 分区 / 学生搜索框 / 座位 63 座 / 请假 / 值日 / 周末 / 课表 / 任务 / 控制中心 / 同步诊断卡 / 课堂三卡 + 返回） |
| 随机点名        | 连点 5 次只滚一轮，定格真实学生「陈思远」；按钮变「再抽一次」                                                                      |
| 抽签            | 连点 5 次只滚一轮，定格真实值日组「第 3 组」                                                                                       |
| 计时器          | 双击「开始」不被读成暂停（按钮防抖生效）；00:59 → 00:58 → 00:57 → 00:56 每秒精确 −1；暂停 2 秒读数不变；重置回 01:00               |
| 返回入口        | 「返回工作台」在位，点击回到工作台                                                                                                 |
| Excel 座位导入  | 真喂一份 xlsx：3 行全部识别（含两名重名按学号匹配）→ 确认 → 提示「导入完成：3 个座位，3 名学生换位」→ 弹窗关闭 → 座位真的写入      |
| 性能            | 路由标题可见 214–591ms；学生搜索 11ms（6→1 张卡）；点名 1 秒内刷新 **67 帧**；计时器每秒稳定 −1s                                   |
| 自检            | **554 条**全绿（v3.0.0 新增 sheetCell 用例 9 条）；prettier / eslint / vue-tsc / build 全绿                                        |
| CI              | `.github/workflows/ci.yml` 在 push / PR / tag 上跑同一条链                                                                         |

**未覆盖（如实说明）**：真实 CloudBase 环境下的端到端同步未在本次验证中跑（需要线上账号与网络），
本阶段靠「假云端 + 双设备模拟」的常驻自检覆盖同一套逻辑；真机 CloudBase 验证属于上线当天的人工步骤（见第 3、10 节）。

## 附 C：`v3.3.1-rc` 专项核对（本次）

> 常规六大项之外的**本版专有项**。需求原文第八节「验收标准」逐条对应在这里。

### 课程表

- [ ] 「我的 → 教学设置 → 课程」有红色危险行「删除所有课程」，副标题写明「保留课程时间设置」
- [ ] 点击弹二次确认，正文一字不差：「将删除课程表中的全部课程，此操作不可撤销。」
- [ ] 确认后课表清空、**换课记录一并清掉**（原先「已调走」的标记不残留）
- [ ] **课程时间设置原样保留**：清空前先在「课程时间设置」里改一节（如早自习 07:30），清空后仍是 07:30
- [ ] 周视图画满七天：**没课的周六 / 周日也能点空格子新增**
- [ ] 周六 / 周日的课能编辑、能删除，行为与工作日一致
- [ ] 把系统日期改到周六（或周日）打开首页：**「今日课程」显示当天的课**，不是空态

### SeatPlan

- [ ] 「更多」点得开（触发器**不再整体灰掉**），菜单里能看到：自动排座 / 方案对比 ｜ 重命名 / 复制 / 删除方案
- [ ] 重命名、复制、删除三个动作各自生效（删除后切回另一份方案；只剩一份时该项灰掉并有悬停说明）
- [ ] 「导出座位图」点得开（原先是工具栏 `overflow: hidden` 把下拉剪没了）
- [ ] PNG 老师视角 / 学生视角各导一次，**讲台 / 前门 / 后门 / 窗 / 过道 / 学生姓名都在**，A4 横向比例
- [ ] **前门与讲台在同一水平线上**，不压住第 1 排（两个视角都看一遍）
- [ ] 座位格比上一版小约 10%，**姓名完整显示**（重名的带尾号，不被省略号截断）
- [ ] **两条过道明显更宽**，9 列看得出是「3 组三人」而不是一排九个

### 学生档案

- [ ] 新增 / 编辑表单有「身份证尾号」（选填，最多 4 位），提示写明「只在重名时显示」
- [ ] 详情弹窗有「身份证尾号」一行；没填的显示「—」
- [ ] Excel 导入能认「身份证尾号 / 身份证后四位 / 证件尾号 / 尾号」这几种表头，预览表有这一列
- [ ] 填两个同名学生的尾号 → 卡片 / 详情 / 座位图 / 导出图**处处显示 `张三（4321）`**
- [ ] 不重名的学生**只显示姓名**（哪怕填了尾号）
- [ ] **「同名 N 人」「重名几人」「第 X 组」这几类文案全站都搜不到**

### 首页

- [ ] Hero **标题字号没变**，Hero 以下的分区标题 / 副标题 / 快捷入口 / 班级动态 / 今日课程**都大了一号**
- [ ] 今日课程卡：科目与时间的数字最醒目，一行行读下来不费劲

### 工程

- [ ] 六步全绿；`npm run test` **666 条 / 30 个文件**
- [ ] `v331Linkage.test.ts` 的 14 项在里面（接缝专项）
- [ ] **`LC_ALL=en_US.UTF-8 npm run test` 同样全绿**——姓名 / 标签排序必须写明 `zh-Hans-CN`，
      否则本机（zh-CN）绿、CI（en-US）红（见 CHANGELOG「交付时修掉的一条静默缺陷」）
